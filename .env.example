DATABASE_URL="file:./dev.db"
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_REDIRECT_URI="http://localhost:3000/api/auth/google/callback"
APP_BASE_URL="http://localhost:3000"
APP_ENCRYPTION_KEY=""  # 32 bytes, base64 (openssl rand -base64 32)
# App password gate (protects the app from public access)
APP_PASSWORD=""          # the password Beni uses to log in
SESSION_SECRET=""        # random string for signing session cookie (openssl rand -base64 32)
# Anthropic API Config (Optional - required if using Claude provider)
ANTHROPIC_API_KEY=""
ANTHROPIC_MODEL="claude-3-5-sonnet-latest"

